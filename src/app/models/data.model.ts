export interface user {
  id: string;
  name: string;
  pin: number;
}

export interface Engagementcomponent {
  question: string;
}

export interface Datapoll {
  roomId: string;
  idPoll: string;
}
